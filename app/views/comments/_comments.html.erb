<% comments.includes(:parent, {comments:[:parent]}, :user).each do |comment|%>
    <%= turbo_frame_tag comment do %>
      <% unless comment.parent %>
      <hr class="border border-2 opacity-75">
        <%= render 'comments/comment', comment: comment, post: post %>  
        
      <% end %>    
    <% end %> 
<% end %>


