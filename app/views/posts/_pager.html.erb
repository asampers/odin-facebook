<div id="pager">
  <% if @posts.next_page %>
    <%= link_to(
          "Load more cards",
          posts_path(page: (@posts.next_page)),
          class:"invisible",
          data: {
            turbo_stream: "",
            controller: "autoclick"
          }
        ) %>
  <% end %>
</div>
