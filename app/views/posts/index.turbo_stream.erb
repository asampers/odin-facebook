<%= turbo_stream_action_tag(
  "append",
  target: "posts",
  template: %(#{render @posts}) 
) %>
<%= turbo_stream_action_tag(
  "replace",
  target: "pager",
  template: %(#{render "pager", posts: @posts})
) %>