<% content_for :right_bottom do %>
  <div class="container mt-3 mb-4 p-3 border shadow-sm rounded post">
    <h4 class="text-center">Newest Users</h4>
      <% User.by_recently_created.limit(5).each do |user| %>
        <div>
          <%= image_tag user.gravatar_url(default: :identicon), class:"rounded-circle img-fluid gravatar-home me-2 mb-1" %>
          <%= link_to "#{user.username}", user_path(user.id), class: "me-2" %> 
        </div> 
      <% end %>
  </div>
<% end %>  