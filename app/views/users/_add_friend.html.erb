<%= form_with scope: :friendship, url: friendships_path do |f| %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :friend_id, value: user.id %>
    <%= f.submit "Add Friend", class: "btn btn-primary" %>
<% end %>